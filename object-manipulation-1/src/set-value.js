/* exported setValue */
// in this procedure, we will be adding onto the object thats provided, by adding info that's provided.
// this is accomplished by setting a new object property through bracket notation and adding new value to it.
// no return is necessary.
function setValue(object, key, value) {
  object[key] = value;
}
