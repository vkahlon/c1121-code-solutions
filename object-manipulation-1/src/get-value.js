/* exported getValues */
// This procedure returns the value found in the key property
// it returns  value of the object
// from the property accesor at key of the object.
function getValue(object, key) {
  return object[key];
}
