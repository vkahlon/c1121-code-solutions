/* exported last */
// In this procedure, we will be taking in an array, find its length, and subtract it by one
// from their it will return its last index.
// Utilize the input array, find its length, subtract it by 1,
// return the value of the array AT the length minus 1.
function last(array) {
  return array[array.length - 1];
}
