/* exported head */
// In this procedure, we will be taking in an array input and returning its first value.
// This is accomplished by taking the array at its first index and returning its value.
function head(array) {
  return array[0];
}
