/* exported getFirstChar */
function getFirstChar(string) { // function //
  return string.charAt(0); // Utilizing the charAt procedure in order to generate a new word that contains the first letter of the word that was put in.
}
